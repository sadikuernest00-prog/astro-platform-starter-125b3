---
const featuresBorrower = [
  "Request crypto loans directly from real lenders.",
  "Use stablecoins like USDT, USDC or DAI — wallet to wallet.",
  "Back your loan with crypto collateral or tokenized real assets.",
  "Negotiate terms and repay transparently on–chain.",
];

const featuresLender = [
  "Earn yield by lending stablecoins to real people and projects.",
  "Invest in tokenized deals backed by real–world assets.",
  "Always keep crypto in your own wallet — no custody, no banks.",
  "Choose your risk level, yield, and duration.",
];

// Your initial price object
let prices = {
  BTC: null,
  ETH: null,
  SOL: null,
};
---

<main style="
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  font-family: system-ui, sans-serif;
  padding: 2rem;
  background: #020617;
  color: #e5e7eb;
  text-align: center;
">
  <section style="
    max-width: 720px;
    width: 100%;
    background: rgba(15, 23, 42, 0.85);
    border-radius: 1.5rem;
    padding: 2.5rem 2rem;
    border: 1px solid rgba(148, 163, 184, 0.3);
    box-shadow: 0 24px 60px rgba(0, 0, 0, 0.45);
    backdrop-filter: blur(14px);
  ">
    <h1 style="font-size: 2.6rem; margin-bottom: 1.2rem; font-weight: 700;">
      Amicbridge
    </h1>

    <p style="color:#cbd5e1; margin-bottom:1.5rem;">
      Peer-to-peer lending with crypto. No banks. No intermediaries.
    </p>

    <h2 style="font-size:1.4rem; margin-top:2rem; margin-bottom:0.75rem;">
      Borrower Features
    </h2>
    <ul style="text-align:left; margin:auto; max-width:500px; line-height:1.6;">
      {featuresBorrower.map((item) => (
        <li>{item}</li>
      ))}
    </ul>

    <h2 style="font-size:1.4rem; margin-top:2rem; margin-bottom:0.75rem;">
      Lender Features
    </h2>
    <ul style="text-align:left; margin:auto; max-width:500px; line-height:1.6;">
      {featuresLender.map((item) => (
        <li>{item}</li>
      ))}
    </ul>

    <h2 style="font-size:1.4rem; margin-top:2rem; margin-bottom:0.75rem;">
      Crypto Prices
    </h2>

    <div id="price-box" style="margin-top:1rem; color:#94a3b8;">
      Loading prices…
    </div>
  </section>

  <footer style="margin-top:2rem; font-size:0.85rem; color:#94a3b8;">
    Built with Astro — no SolidJS required 🚀
  </footer>
</main>

<!-- Runs after page load -->
<script>
  document.addEventListener("DOMContentLoaded", async () => {
    // Example: fetch prices and update DOM
    try {
      const res = await fetch(
        "https://api.coingecko.com/api/v3/simple/price?ids=bitcoin,ethereum,solana&vs_currencies=usd"
      );
      const data = await res.json();

      const prices = {
        BTC: data.bitcoin?.usd,
        ETH: data.ethereum?.usd,
        SOL: data.solana?.usd,
      };

      const el = document.getElementById("price-box");
      el.innerHTML = `
        <div>BTC: $${prices.BTC}</div>
        <div>ETH: $${prices.ETH}</div>
        <div>SOL: $${prices.SOL}</div>
      `;
    } catch (err) {
      document.getElementById("price-box").innerHTML =
        "Failed to load price data.";
      console.error(err);
    }
  });
</script>
