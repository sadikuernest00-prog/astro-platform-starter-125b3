---
const year = new Date().getFullYear();
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>AmicBridge – Borrow, Lend & Investment Strategy Requests</title>
    <meta
      name="description"
      content="AmicBridge lets people send requests to borrow, lend, or ask for paid investment strategy advice in a transparent peer-to-peer way."
    />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
  </head>

  <body class="page">
    <header class="header">
      <div class="container header-inner">
        <div class="logo">
          <span class="logo-mark">A</span>
          <span class="logo-text">AmicBridge</span>
        </div>

        <nav class="nav">
          <a href="#borrow" class="nav-link">Borrow</a>
          <a href="#lend" class="nav-link">Lend</a>
          <a href="#strategy" class="nav-link">Investment strategy</a>
        </nav>
      </div>
    </header>

    <main>
      <!-- HERO -->
      <section class="hero">
        <div class="container hero-inner">
          <div class="hero-text">
            <p class="hero-tagline">Simple peer-to-peer finance</p>
            <h1>Borrow, lend, and request investment strategy in one place.</h1>
            <p class="hero-subtitle">
              AmicBridge is a concept platform where people can send clear requests:
              ask for a loan, offer to lend, or pay for investment strategy
              guidance. Every request starts with a simple form – no hype, no
              hidden promises.
            </p>

            <div class="hero-buttons">
              <a href="#borrow" class="btn btn-primary">Request a loan</a>
              <a href="#strategy" class="btn btn-ghost">Request strategy advice</a>
            </div>

            <p class="hero-note">
              Important: AmicBridge does not guarantee any loan, profit, or result.
              All finance involves risk, and you are responsible for your decisions.
            </p>
          </div>
        </div>
      </section>

      <!-- THREE REQUEST TYPES -->
      <section class="section">
        <div class="container cards-grid">
          <!-- BORROW CARD -->
          <article id="borrow" class="card">
            <h2>Borrow money from people</h2>
            <p class="card-text">
              Send a request to potential lenders with the amount you need, how long
              you want to borrow, and how you plan to repay.
            </p>

            <form class="form" method="post" action="#">
              <label>
                Name
                <input type="text" name="borrower_name" placeholder="Your full name" required />
              </label>

              <label>
                Email
                <input type="email" name="borrower_email" placeholder="you@example.com" required />
              </label>

              <div class="form-row">
                <label>
                  Amount wanted
                  <input type="number" name="borrow_amount" placeholder="10000" min="1" required />
                </label>
                <label>
                  Currency
                  <select name="borrow_currency">
                    <option value="EUR">EUR</option>
                    <option value="USD">USD</option>
                    <option value="GBP">GBP</option>
                    <option value="NOK">NOK</option>
                  </select>
                </label>
              </div>

              <label>
                How long do you want to borrow?
                <select name="borrow_duration">
                  <option value="6m">Up to 6 months</option>
                  <option value="12m">Up to 12 months</option>
                  <option value="24m">Up to 24 months</option>
                  <option value="36m">Up to 36 months</option>
                </select>
              </label>

              <label>
                Max interest you can accept (per year)
                <input type="number" name="borrow_rate" placeholder="10" min="1" max="40" />
              </label>

              <label>
                What is the loan for?
                <textarea
                  name="borrow_purpose"
                  rows="3"
                  placeholder="Explain why you need the money and how you plan to repay."
                ></textarea>
              </label>

              <button type="submit" class="btn btn-primary btn-full">
                Send borrow request
              </button>

              <p class="card-note">
                This is only a request. Lenders are free to say no, and no money is
                moved automatically.
              </p>
            </form>
          </article>

          <!-- LEND CARD -->
          <article id="lend" class="card">
            <h2>Offer to lend money</h2>
            <p class="card-text">
              Share how much you want to lend, your expected interest rate, and what
              kind of borrowers you prefer.
            </p>

            <form class="form" method="post" action="#">
              <label>
                Name
                <input type="text" name="lender_name" placeholder="Your full name" required />
              </label>

              <label>
                Email
                <input type="email" name="lender_email" placeholder="you@example.com" required />
              </label>

              <div class="form-row">
                <label>
                  Amount to lend
                  <input type="number" name="lend_amount" placeholder="5000" min="1" required />
                </label>
                <label>
                  Currency
                  <select name="lend_currency">
                    <option value="EUR">EUR</option>
                    <option value="USD">USD</option>
                    <option value="GBP">GBP</option>
                    <option value="NOK">NOK</option>
                  </select>
                </label>
              </div>

              <label>
                Target interest (per year)
                <input type="number" name="lend_rate" placeholder="8" min="1" max="40" />
              </label>

              <label>
                What kind of borrowers do you prefer?
                <textarea
                  name="lend_profile"
                  rows="3"
                  placeholder="Example: small businesses, private persons, with or without collateral, etc."
                ></textarea>
              </label>

              <button type="submit" class="btn btn-secondary btn-full">
                Send lending offer
              </button>

              <p class="card-note">
                Lending is risky. Borrowers can fail to pay back. You should never
                lend more than you can afford to lose.
              </p>
            </form>
          </article>

          <!-- STRATEGY CARD -->
          <article id="strategy" class="card">
            <h2>Request investment strategy advice</h2>
            <p class="card-text">
              Send a paid request to get personal investment strategy ideas. You
              describe your situation and how much you are willing to pay for advice.
            </p>

            <form class="form" method="post" action="#">
              <label>
                Name
                <input type="text" name="strategy_name" placeholder="Your full name" required />
              </label>

              <label>
                Email
                <input type="email" name="strategy_email" placeholder="you@example.com" required />
              </label>

              <label>
                Budget for strategy advice
                <input
                  type="number"
                  name="strategy_budget"
                  placeholder="200"
                  min="1"
                  required
                />
              </label>

              <label>
                What is your investment goal?
                <textarea
                  name="strategy_goal"
                  rows="3"
                  placeholder="Example: long-term growth, saving for house, balancing risk, etc."
                ></textarea>
              </label>

              <label>
                What can you invest in? (optional)
                <textarea
                  name="strategy_assets"
                  rows="2"
                  placeholder="Example: stocks, funds, crypto, cash, etc."
                ></textarea>
              </label>

              <button type="submit" class="btn btn-ghost btn-full">
                Send strategy request
              </button>

              <p class="card-note">
                Any strategy ideas you receive are not guarantees. Markets can go up
                and down, and nobody can promise profit.
              </p>
            </form>
          </article>
        </div>
      </section>
    </main>

    <footer class="footer">
      <div class="container footer-inner">
        <p class="footer-main">
          AmicBridge is a conceptual peer-to-peer platform design. It does not act
          as a bank or licensed adviser. All examples are for information only.
        </p>
        <p class="footer-meta">
          © {year} AmicBridge · Your decisions, your responsibility.
        </p>
      </div>
    </footer>
  </body>

  <style>
    :global(body) {
      margin: 0;
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
        sans-serif;
      background: radial-gradient(circle at top, #1e293b, #020617);
      color: #f9fafb;
    }

    .page {
      min-height: 100vh;
    }

    .container {
      max-width: 1100px;
      margin: 0 auto;
      padding: 0 1.5rem;
    }

    .header {
      position: sticky;
      top: 0;
      z-index: 20;
      backdrop-filter: blur(16px);
      background: rgba(2, 6, 23, 0.9);
      border-bottom: 1px solid rgba(148, 163, 184, 0.25);
    }

    .header-inner {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0.9rem 0;
    }

    .logo {
      display: flex;
      align-items: center;
      gap: 0.6rem;
      font-weight: 600;
    }

    .logo-mark {
      width: 30px;
      height: 30px;
      border-radius: 999px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      background: radial-gradient(circle at 30% 20%, #38bdf8, #6366f1, #0f172a);
      box-shadow: 0 0 18px rgba(59, 130, 246, 0.7);
      font-size: 0.9rem;
    }

    .logo-text {
      font-size: 1.05rem;
    }

    .nav {
      display: flex;
      gap: 1rem;
      font-size: 0.9rem;
    }

    .nav-link {
      color: #e5e7eb;
      text-decoration: none;
      padding-bottom: 0.1rem;
      border-bottom: 2px solid transparent;
      transition: border-color 0.15s ease, color 0.15s ease;
    }

    .nav-link:hover {
      border-color: #6366f1;
      color: #ffffff;
    }

    .hero {
      padding: 3.2rem 0 2.2rem;
    }

    .hero-inner {
      max-width: 760px;
    }

    .hero-text h1 {
      font-size: clamp(2rem, 3vw, 2.6rem);
      line-height: 1.1;
      margin: 0.4rem 0 0.7rem;
    }

    .hero-tagline {
      text-transform: uppercase;
      letter-spacing: 0.18em;
      font-size: 0.8rem;
      color: #a5b4fc;
    }

    .hero-subtitle {
      color: #cbd5f5;
      font-size: 0.98rem;
      margin-bottom: 1.5rem;
    }

    .hero-buttons {
      display: flex;
      gap: 0.9rem;
      margin-bottom: 1rem;
      flex-wrap: wrap;
    }

    .hero-note {
      font-size: 0.8rem;
      color: #9ca3af;
      max-width: 32rem;
    }

    .btn {
      border-radius: 999px;
      padding: 0.5rem 1.1rem;
      border: 1px solid transparent;
      font-size: 0.9rem;
      cursor: pointer;
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      transition: background 0.15s ease, border-color 0.15s ease,
        transform 0.1s ease, box-shadow 0.15s ease;
    }

    .btn-primary {
      background: linear-gradient(to right, #6366f1, #22c55e);
      color: #ffffff;
      box-shadow: 0 14px 35px rgba(79, 70, 229, 0.45);
    }

    .btn-primary:hover {
      transform: translateY(-1px);
      box-shadow: 0 18px 50px rgba(79, 70, 229, 0.6);
    }

    .btn-secondary {
      background: rgba(15, 23, 42, 0.96);
      border-color: rgba(148, 163, 184, 0.7);
      color: #e5e7eb;
    }

    .btn-ghost {
      background: transparent;
      border-color: rgba(148, 163, 184, 0.7);
      color: #e5e7eb;
    }

    .btn-ghost:hover,
    .btn-secondary:hover {
      border-color: #6366f1;
      background: rgba(15, 23, 42, 0.98);
    }

    .btn-full {
      width: 100%;
      margin-top: 0.5rem;
    }

    .section {
      padding: 2.5rem 0 3rem;
    }

    .cards-grid {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 1.5rem;
    }

    .card {
      border-radius: 1rem;
      border: 1px solid rgba(148, 163, 184, 0.3);
      background: radial-gradient(circle at top left, #111827, #020617);
      padding: 1.4rem 1.3rem 1.2rem;
      box-shadow: 0 18px 40px rgba(15, 23, 42, 0.9);
      font-size: 0.92rem;
    }

    .card h2 {
      margin-top: 0;
      font-size: 1.1rem;
      margin-bottom: 0.4rem;
    }

    .card-text {
      color: #cbd5f5;
      margin-bottom: 0.9rem;
    }

    .card-note {
      margin-top: 0.6rem;
      font-size: 0.75rem;
      color: #9ca3af;
    }

    .form {
      display: flex;
      flex-direction: column;
      gap: 0.55rem;
      font-size: 0.85rem;
    }

    label {
      display: flex;
      flex-direction: column;
      gap: 0.35rem;
      font-size: 0.78rem;
      text-transform: uppercase;
      letter-spacing: 0.12em;
      color: #9ca3af;
    }

    input,
    select,
    textarea {
      border-radius: 0.7rem;
      border: 1px solid rgba(148, 163, 184, 0.4);
      background: rgba(15, 23, 42, 0.95);
      padding: 0.55rem 0.7rem;
      color: #e5e7eb;
      font: inherit;
      outline: none;
      resize: vertical;
      transition: border-color 0.12s ease, box-shadow 0.12s ease,
        background 0.12s ease;
    }

    input:focus,
    select:focus,
    textarea:focus {
      border-color: #6366f1;
      box-shadow: 0 0 0 1px rgba(79, 70, 229, 0.7);
      background: #020617;
    }

    .form-row {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 0.6rem;
    }

    .footer {
      border-top: 1px solid rgba(148, 163, 184, 0.35);
      padding: 1.2rem 0 1.5rem;
      background: #020617;
      font-size: 0.8rem;
      color: #9ca3af;
      margin-top: 0.5rem;
    }

    .footer-inner {
      display: flex;
      justify-content: space-between;
      gap: 1rem;
      align-items: center;
    }

    .footer-main {
      max-width: 32rem;
    }

    .footer-meta {
      text-align: right;
    }

    @media (max-width: 960px) {
      .header-inner {
        flex-direction: column;
        align-items: flex-start;
        gap: 0.6rem;
      }

      .nav {
        flex-wrap: wrap;
      }

      .cards-grid {
        grid-template-columns: minmax(0, 1fr);
      }

      .footer-inner {
        flex-direction: column;
        align-items: flex-start;
      }

      .footer-meta {
        text-align: left;
      }
    }
  </style>
</html>
